{
  "name": "gutenberg-format-convert",
  "version": "1.0.0",
  "description": "book.html contains a Gutenberg book in HTML format. index.html reads that book and converts it to markdown.",
  "main": "convert.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^19.8.0",
    "turndown": "^7.1.2"
  }
}
